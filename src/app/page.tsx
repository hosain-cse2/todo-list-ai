import { Heading } from "@/components/ui/Heading";

export default function Home() {
  return (
    <main style={{ padding: 24 }}>
      <Heading>Todo AI</Heading>
      <p>Frontend + Backend in one Next.js app</p>
    </main>
  );
}
